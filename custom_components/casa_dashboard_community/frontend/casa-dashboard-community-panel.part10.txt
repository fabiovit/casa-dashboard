px;transform:translateX(-50%);border:6px solid #596a76;background:#173246}.office-chair{position:absolute;left:50%;bottom:8px;width:64px;height:55px;transform:translateX(-50%);border-radius:16px 16px 8px 8px;background:#3f4c55}.office-lamp{position:absolute;right:28px;bottom:58px;width:5px;height:92px;background:#7b8993}
      .laundry-machine{position:absolute;left:45px;bottom:28px;width:112px;height:142px;border-radius:10px;background:#d0d5d9}.laundry-door{position:absolute;left:21px;top:45px;width:68px;height:68px;border-radius:50%;background:#172b38;border:9px solid #7d8c95}.laundry-shelf{position:absolute;right:34px;top:38px;width:130px;height:12px;background:#63717a;box-shadow:0 48px 0 #63717a}.laundry-basket{position:absolute;right:58px;bottom:26px;width:78px;height:70px;background:#465762;clip-path:polygon(8% 0,92% 0,100% 100%,0 100%)}
      .hobby-table{position:absolute;left:46px;right:46px;bottom:38px;height:18px;background:#755f48}.hobby-cabinet{position:absolute;right:34px;top:32px;width:92px;height:108px;background:#4b5359}.hobby-lamp{position:absolute;left:50%;top:0;width:4px;height:72px;background:#7b8993}.hobby-lamp:after{content:"";position:absolute;left:-25px;bottom:-8px;width:54px;height:28px;border-radius:50%;background:#d4ae62}
      .generic-window{position:absolute;left:34px;top:30px;width:120px;height:90px;border:6px solid #63737e;background:#1d4054}.generic-table{position:absolute;left:48px;right:48px;bottom:38px;height:16px;background:#687781}.generic-lamp{position:absolute;right:40px;top:22px;width:5px;height:95px;background:#7c8b95}
      .stairs-hero-scene{position:relative;height:220px;border-radius:20px;overflow:hidden;background:linear-gradient(180deg,#17232b,#0c1217)}.stairs-wall{position:absolute;inset:0;background:linear-gradient(135deg,#222e36,#131a20);clip-path:polygon(0 0,100% 0,100% 100%,38% 100%)}.stairs-flight{position:absolute;left:8%;bottom:10%;width:62%;height:66%;display:flex;flex-direction:column-reverse;gap:4px;transform:skewY(-18deg)}.stairs-flight i{height:13px;background:linear-gradient(90deg,#53636f,#29353e);border-top:1px solid #738592}.stairs-rail{position:absolute;left:19%;top:12%;width:5px;height:68%;background:#7f929e;transform:rotate(27deg);transform-origin:bottom}.stairs-door{position:absolute;right:9%;bottom:8%;width:68px;height:118px;border:5px solid #5f707d;background:#222c34}
      @media(max-width:980px){.ui-v2 .original-room-hero{grid-template-columns:1fr!important}.original-room-hero-card{grid-template-columns:1fr}.original-room-side{display:grid;grid-template-columns:repeat(3,1fr)}}@media(max-width:620px){.original-room-hero-card{padding:14px}.original-room-side{grid-template-columns:1fr}.community-original-scene{height:185px!important}}

      /* v2.0.0 - restore and emphasize original Casa V1 iconography */
      .ui-v1 .command-symbol ha-icon{--mdc-icon-size:23px!important}
      .ui-v1 .command-symbol{color:#8ba5bd}
      .ui-v1 .command-strip.state-on .command-symbol,.ui-v1 .command-symbol.active{color:#55e6ad}
      .ui-v1 .scene-mark ha-icon{--mdc-icon-size:24px}

      /* v2.0.0 - original iconography in Casa V2 */
      .ui-v2 .light-visual-toggle ha-icon,
      .ui-v2 .universal-device-action ha-icon,
      .ui-v2 .generic-sensor ha-icon{--mdc-icon-size:22px}
      .ui-v2 .scene-icon ha-icon{--mdc-icon-size:25px}

      /* v2.0.0 - per-room custom display names */
      .room-entity-chip-wrap{display:flex;flex-direction:column;gap:5px;min-width:185px;max-width:100%}
      .room-entity-alias{display:grid;grid-template-columns:auto minmax(0,1fr);gap:7px;align-items:center;padding:0 2px}
      .room-entity-alias>span{font-size:8px;letter-spacing:.08em;text-transform:uppercase;color:var(--secondary-text-color);font-weight:800}
      .room-entity-alias input{min-width:0!important;padding:6px 8px!important;font-size:10px!important;border-radius:8px!important}
      .room-entity-chips{align-items:flex-start}

      /* v2.0.0 - dedicated Air Fryer visual */
      .uv-airfryer{width:132px;height:132px;display:grid;place-items:center;position:relative}
      .uv-airfryer-body{position:relative;width:88px;height:112px;border-radius:22px 22px 18px 18px;background:linear-gradient(155deg,#56636c,#222b31 55%,#151b20);border:2px solid #667781;box-shadow:0 18px 28px rgba(0,0,0,.28)}
      .uv-airfryer-top{position:absolute;left:14px;right:14px;top:9px;height:20px;border-radius:10px;background:#1a2227;display:flex;align-items:center;justify-content:center;gap:9px}
      .uv-airfryer-top i{width:7px;height:7px;border-radius:50%;border:1px solid #7e919d}
      .uv-airfryer-display{position:absolute;left:23px;right:23px;top:34px;height:20px;border-radius:6px;background:#0d171d;display:grid;place-items:center;color:#7994a4;font-size:7px;letter-spacing:.12em}
      .uv-airfryer.active .uv-airfryer-display{color:#57e7ae;box-shadow:0 0 15px rgba(87,231,174,.16) inset}
      .uv-airfryer-basket{position:absolute;left:13px;right:13px;bottom:17px;height:42px;border-radius:7px 7px 13px 13px;background:#202a30;border:1px solid #596973}
      .uv-airfryer-window{position:absolute;left:10px;right:10px;top:8px;height:20px;border-radius:5px;background:linear-gradient(180deg,#182832,#0c151a)}
      .uv-airfryer-handle{position:absolute;left:50%;bottom:-17px;width:36px;height:20px;transform:translateX(-50%);border-radius:0 0 9px 9px;background:#333f46}
      .uv-airfryer-heat{position:absolute;left:50%;bottom:7px;transform:translateX(-50%);display:flex;gap:5px;opacity:0}
      .uv-airfryer.active .uv-airfryer-heat{opacity:1}
      .uv-airfryer-heat i{width:3px;height:13px;border-radius:999px;background:#ffb454;box-shadow:0 0 9px rgba(255,180,84,.45)}


      /* v3.1.0 - balanced configurator scrolling */
      .config-dialog{
        width:min(1120px,calc(100vw - 28px)) !important;
        height:min(94vh,980px) !important;
        max-height:min(94vh,980px) !important;
        display:flex !important;
        flex-direction:column !important;
        overflow:hidden !important;
      }
      .config-dialog-head,
      .config-toolbar,
      .config-dialog-foot{
        flex:0 0 auto !important;
      }

      /* Rooms remain comfortably usable without consuming the whole modal. */
      .room-manager{
        flex:0 0 auto !important;
        display:flex !important;
        flex-direction:column !important;
        max-height:min(38vh,410px) !important;
        margin:0 16px 12px !important;
        padding:14px 16px !important;
        overflow:hidden !important;
      }
      .room-manager-head,
      .room-friendly-hint{
        flex:0 0 auto !important;
      }
      .room-list{
        flex:1 1 auto !important;
        min-height:125px !important;
        max-height:min(27vh,285px) !important;
        overflow-y:auto !important;
        overflow-x:hidden !important;
        padding-right:8px !important;
        overscroll-behavior:contain;
        -webkit-overflow-scrolling:touch;
        scrollbar-gutter:stable;
      }

      /* The 173 mappings always keep a useful area of their own. */
      .config-toolbar{
        padding-top:12px !important;
        padding-bottom:12px !important;
      }
      .config-scroll{
        flex:1 1 320px !important;
        min-height:220px !important;
        max-height:none !important;
        overflow-y:auto !important;
        overflow-x:hidden !important;
        overscroll-behavior:contain;
        -webkit-overflow-scrolling:touch;
        scrollbar-gutter:stable;
        padding-bottom:26px !important;
      }
      .config-dialog-foot{
        position:relative !important;
        z-index:20;
        background:color-mix(in srgb,var(--card-background-color) 98%,transparent) !important;
      }

      @media (max-height:800px) and (min-width:761px){
        .config-dialog{
          height:96vh !important;
          max-height:96vh !important;
        }
        .room-manager{
          max-height:32vh !important;
        }
        .room-list{
          min-height:105px !important;
          max-height:22vh !important;
        }
        .config-scroll{
          min-height:190px !important;
        }
      }

      @media (max-width:760px){
        .config-dialog{
          width:100vw !important;
          height:100dvh !important;
          max-height:100dvh !important;
          border-radius:0 !important;
        }
        .room-manager{
          margin:0 8px 10px !important;
          max-height:38dvh !important;
        }
        .room-list{
          min-height:120px !important;
          max-height:27dvh !important;
        }
        .config-scroll{
          min-height:30dvh !important;
          flex-basis:34dvh !important;
        }
      }

      /* v3.1.0 - adaptive Community overview */
      .alert.info{border-color:color-mix(in srgb,var(--primary-color) 22%,var(--divider-color));background:color-mix(in srgb,var(--primary-color) 5%,var(--card-background-color))}
      .alert.info ha-icon{color:var(--primary-color)}
      .alert.info .config-open-btn{border-color:color-mix(in srgb,var(--primary-color) 34%,var(--divider-color));background:color-mix(in srgb,var(--primary-color) 8%,var(--card-background-color))}
      .alert.info .config-open-btn ha-icon{color:var(--primary-color)}

      .overview-key-grid{
        display:grid!important;
        grid-template-columns:repeat(auto-fit,minmax(220px,1fr))!important;
        gap:14px!important;
        margin:14px 0 32px!important;
      }
      .overview-key-card{
        min-height:108px;
        padding:16px!important;
        display:flex;
        align-items:center;
        gap:14px;
        border:1px solid color-mix(in srgb,var(--primary-color) 13%,var(--divider-color))!important;
        border-radius:19px!important;
        background:linear-gradient(145deg,
          color-mix(in srgb,var(--card-background-color) 96%,var(--primary-color) 4%),
          color-mix(in srgb,var(--secondary-background-color) 94%,var(--primary-color) 6%))!important;
        box-shadow:0 10px 28px color-mix(in srgb,#000 10%,transparent);
        overflow:hidden;
      }
      .overview-key-card.solar{background:linear-gradient(145deg,color-mix(in srgb,var(--card-background-color) 94%,#f6b73c 6%),color-mix(in srgb,var(--secondary-background-color) 96%,#f6b73c 4%))!important}
      .overview-key-card.wallbox{background:linear-gradient(145deg,color-mix(in srgb,var(--card-background-color) 94%,var(--primary-color) 6%),color-mix(in srgb,var(--secondary-background-color) 95%,var(--primary-color) 5%))!important}
      .overview-key-card.external{background:linear-gradient(145deg,color-mix(in srgb,var(--card-background-color) 94%,#5eaefe 6%),color-mix(in srgb,var(--secondary-background-color) 96%,#5eaefe 4%))!important}
      .overview-key-icon{
        width:54px;height:54px;border-radius:16px;display:grid;place-items:center;flex:none;
        color:var(--primary-color);
        border:1px solid color-mix(in srgb,var(--primary-color) 25%,var(--divider-color));
        background:color-mix(in srgb,var(--primary-color) 8%,var(--card-background-color));
      }
      .overview-key-card.solar .overview-key-icon{color:#d69614;border-color:color-mix(in srgb,#d69614 30%,var(--divider-color));background:color-mix(in srgb,#f6b73c 10%,var(--card-background-color))}
      .overview-key-icon ha-icon{--mdc-icon-size:27px}
      .overview-key-copy{min-width:0;flex:1}
      .overview-key-card .overview-key-copy>span{
        display:block!important;font-size:10px!important;letter-spacing:.1em!important;
        color:var(--secondary-text-color)!important;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;
      }
      .overview-key-value{display:flex;align-items:baseline;gap:7px;margin-top:8px}
      .overview-key-value strong{margin:0!important;font-size:26px!important;letter-spacing:-.035em;color:var(--primary-text-color)}
      .overview-key-value b{font-size:12px;color:var(--secondary-text-color);font-weight:750}
      .theme-light .overview-key-card{box-shadow:0 8px 24px rgba(25,55,80,.08)}
      @media(max-width:650px){
        .overview-key-grid{grid-template-columns:1fr!important}
        .overview-key-card{min-height:94px}
      }
    `;
  }

  _s(id) { const eid=this._eid(id); return eid ? this._hass?.states?.[eid] : null; }
  async _loadEntityConfig(force=false) {
    if((this._entityConfigLoaded && !force) || this._entityConfigLoading) return;
    this._entityConfigLoading=true;
    try {
      let entities=null;
      let labels=null;
      if(this._hass?.callWS){
        try {
          const result=await this._hass.callWS({type:"casa_dashboard_community/config/get"});
          if(result?.entities && typeof result.entities==="object") entities=result.entities;
          if(result?.labels && typeof result.labels==="object") labels=result.labels;
          if(Array.isArray(result?.rooms)) this._rooms=result.rooms;
        } catch(_wsErr) { /* fallback below */ }
      }
      if(!entities){
        const res=await fetch(`${ENTITY_CONFIG_URL}?v=${Date.now()}`, {cache:"no-store"});
        if(res.ok){
          const json=await res.json();
          if(json?.entities && typeof json.entities==="object") entities=json.entities;
          if(json?.labels && typeof json.labels==="object") labels=json.labels;
        }
      }
      this._entityMap=entities || {};
      this._entityLabels=labels || {};
    } catch(_err) {
      this._entityMap={};
      this._entityLabels={};
    } finally {
      this._entityConfigLoaded=true;
      this._entityConfigLoading=false;
      this._renderPage(false);
    }
  }
  _eid(id) {
    if(!ENTITY_KEYS.has(id)) return id;
    const mapped=this._entityMap?.[id];
    return typeof mapped==="string" && mapped.trim() ? mapped.trim() : null;
  }
  _configured(id) { const eid=this._eid(id); return !!eid && !!this._hass?.states?.[eid]; }
  _overviewDisplayName(logicalKey, entityId=null) {
    const custom=String(this._entityLabels?.[logicalKey]||"").trim();
    if(custom) return custom;
    const eid=entityId || this._eid(logicalKey);
    const friendly=eid ? String(this._hass?.states?.[eid]?.attributes?.friendly_name||"").trim() : "";
    if(friendly) return friendly;
    return this._configLabel(logicalKey);
  }
  _configuredCount() { return [...ENTITY_KEYS].filter(id=>this._eid(id)).length; }
  _configNotice() {
    const configured=this._configuredCount();
    const total=ENTITY_KEYS.size;
    const title=configured===0?"Configura le entità":"Entità configurate";
    const text=configured===0
      ? `Sono disponibili ${total} associazioni opzionali. Configura solo le funzioni che utilizzi nel tuo Home Assistant.`
      : `${configured} di ${total} associazioni disponibili configurate. Non è necessario compilarle tutte: configura solo quelle che utilizzi.`;
    const tone=configured===0?"warn":"info";
    const icon=configured===0?"mdi:cog-alert-outline":"mdi:check-circle-outline";
    return `<div class="alert ${tone} config-alert"><ha-icon icon="${icon}"></ha-icon><div><strong>${title}</strong><span>${text}</span></div><button class="config-open-btn" data-action="entity-config" type="button"><ha-icon icon="mdi:tune-variant"></ha-icon> Configura</button></div>`;
  }

  _configLabel(key) {
    const pretty=key.split(".")[1]?.replaceAll("_"," ") || key;
    return pretty.replace(/\b\w/g,c=>c.toUpperCase());
  }
  _roomEntitySuggestions(query="", selected=[]){
    const q=String(query||"").trim().toLocaleLowerCase("it");
    const chosen=new Set(selected||[]);
    return Object.entries(this._hass?.states||{})
      .filter(([id])=>!chosen.has(id))
      .map(([id,obj])=>({id,name:String(obj?.attributes?.friendly_name||id)}))
      .filter(x=>!q || x.id.toLocaleLowerCase("it").includes(q) || x.name.toLocaleLowerCase("it").includes(q))
      .sort((a,b)=>{
        const an=a.name.toLocaleLowerCase("it"), bn=b.name.toLocaleLowerCase("it");
        const ae=an===q?0:an.startsWith(q)?1:an.includes(q)?2:3;
        const be=bn===q?0:bn.startsWith(q)?1:bn.includes(q)?2:3;
        return ae-be || an.localeCompare(bn,"it");
      }).slice(0,12);
  }

  _roomEntityPicker(room){
    const entities=Array.isArray(room.entities)?room.entities:[];
    return `<div class="room-entity-picker" data-room-picker="${room.id}">
      <div class="room-entity-chips">
        ${entities.map(e=>`<div class="room-entity-chip-wrap">
          <span class="room-entity-chip" title="${this._esc(e)}">
            <span>${this._esc(this._roomEntityDisplay(e,room))}</span>
            <small>${this._esc(e)}</small>
            <button type="button" data-room-entity-remove="${room.id}" data-entity="${this._esc(e)}" aria-label="Rimuovi"><ha-icon icon="mdi:close"></ha-icon></button>
          </span>
          <label class="room-entity-alias">
            <span>Nome visualizzato</span>
            <input data-room-entity-label-room="${room.id}" data-room-entity-label-entity="${this._esc(e)}"
              value="${this._esc(this._roomEntityCustomLabel(e,room))}"
              placeholder="${this._esc(this._hass?.states?.[e]?.attributes?.friendly_name||e)}">
          </label>
        </div>`).join("")}
      </div>
      <div class="room-entity-search-wrap">
        <ha-icon icon="mdi:magnify"></ha-icon>
        <input class="room-entity-search" data-room-entity-search="${room.id}" autocomplete="off" placeholder="Cerca per nome o entity_id…">
        <div class="room-entity-suggestions" data-room-entity-suggestions="${room.id}" hidden></div>
      </div>
    </div>`;
  }

  _renderRoomEntitySuggestions(roomId, query=""){
    const layer=this.shadowRoot?.querySelector("#configLayer"); if(!layer)return;
    const room=this._roomsDraft.find(r=>r.id===roomId); if(!room)return;
    const box=layer.querySelector(`[data-room-entity-suggestions="${roomId}"]`); if(!box)return;
    const results=this._roomEntitySuggestions(query,room.entities||[]);
    box.innerHTML=results.length?results.map(x=>`<button type="button" class="room-entity-suggestion" data-room-entity-add="${roomId}" data-entity="${this._esc(x.id)}"><span>${this._esc(x.name)}</span><small>${this._esc(x.id)}</small></button>`).join(""):`<div class="room-entity-noresult">Nessuna entità trovata</div>`;
    const wrap=box.closest(".room-entity-search-wrap");
    const dialog=this.shadowRoot?.querySelector(".config-dialog");
    if(wrap && dialog){
      const wr=wrap.getBoundingClientRect(), dr=dialog.getBoundingClientRect();
      const spaceBelow=dr.bottom-wr.bottom-86;
      const spaceAbove=wr.top-dr.top-20;
      box.classList.toggle("flip-up", spaceBelow<230 && spaceAbove>spaceBelow);
    }
    box.hidden=false;
  }

  _resolveRoomEntityToken(token){
    const raw=String(token||"").trim();
    if(!raw) return "";
    if(this._hass?.states?.[raw]) return raw;
    const needle=raw.toLocaleLowerCase("it");
    const states=Object.entries(this._hass?.states||{});

    const exactFriendly=states.filter(([id,obj]) =>
      String(obj?.attributes?.friendly_name||"").trim().toLocaleLowerCase("it")===needle
    );
    if(exactFriendly.length===1) return exactFriendly[0][0];

    const exactTail=states.filter(([id]) =>
      id.split(".").slice(1).join(".").replaceAll("_"," ").toLocaleLowerCase("it")===needle
    );
    if(exactTail.length===1) return exactTail[0][0];

    const partialFriendly=states.filter(([id,obj]) =>
      String(obj?.attributes?.friendly_name||"").trim().toLocaleLowerCase("it").includes(needle)
    );
    if(partialFriendly.length===1) return partialFriendly[0][0];

    return raw;
  }

  _roomEntityDisplay(entity,room=null){
    const custom=room?.entity_labels && typeof room.entity_labels==="object"
      ? String(room.entity_labels[entity]||"").trim()
      : "";
    if(custom) return custom;
    const obj=this._hass?.states?.[entity];
    return obj?.attributes?.friendly_name || entity;
  }

  _roomEntityCustomLabel(entity,room=null){
    return room?.entity_labels && typeof room.entity_labels==="object"
      ? String(room.entity_labels[entity]||"").trim()
      : "";
  }

  _setRoomEntityLabel(roomId,entity,value){
    const room=this._roomsDraft.find(r=>r.id===roomId);
    if(!room) return;
    room.entity_labels=(room.entity_labels && typeof room.entity_labels==="object") ? {...room.entity_labels} : {};
    const clean=String(value||"").trim();
    if(clean) room.entity_labels[entity]=clean;
    else delete room.entity_labels[entity];
  }

  _legacyRoomForConfigKey(key){
    const full=String(key||"").toLowerCase();
    const name=full.split(".")[1]||"";
    if(name.includes("bathroom") || name.includes("shower")) return "bagno";
    if(name.includes("bedroom") || name.includes("robot_vacuum")) return "camera";
    if(name.includes("kitchen")) return "cucina";
    if(name.includes("hallway")) return "disimpegno";
    if(name.includes("entry_") || full==="alarm_control_panel.home_alarm") return "ingresso";
    if(name.includes("stairs") || name==="main_gate") return "scale";
    if(name==="pedestrian_gate") return "esterno";
    if(name.includes("balcony") || name.includes("weather_") || name.includes("solar_") || name.includes("utility_room") || name.includes("blinds_")) return "balcone";
    if(name.includes("garage_") || name.includes("wallbox") || name.includes("ev_") || name.includes("grid_current")) return "garage";
    const group=String(this._configGroup?.(key)||"").toLocaleLowerCase("it");
    const byGroup={"bagno":"bagno","camera":"camera","cucina":"cucina","disimpegno":"disimpegno","ingresso":"ingresso","scale":"scale","balcone":"balcone","garage":"garage","meteo":"balcone","energia solare":"balcone","wallbox & ev":"garage"};
    return byGroup[group]||"";
  }
  _mergeConfiguredEntitiesIntoRooms(rooms){
    const out=(Array.isArray(rooms)?rooms:[]).map(r=>({...r,entities:[...(r.entities||[])],entity_labels:{...(r.entity_labels||{})}}));
    const defaults=new Map(this._defaultRooms().map(r=>[r.id,r]));
    const configured=[...ENTITY_KEYS].map(key=>{
      const entity=String(this._configDraft?.[key] ?? this._entityMap?.[key] ?? "").trim();
      return {key,entity,room:this._legacyRoomForConfigKey(key)};
    }).filter(x=>x.entity && x.room);

    for(const item of configured){
      const meta=defaults.get(item.room);
      let room=out.find(r=>r.id===item.room)
        || out.find(r=>r.type===item.room)
        || out.find(r=>String(r.name||"").toLocaleLowerCase("it")===String(meta?.name||"").toLocaleLowerCase("it"));
      if(!room && meta){ room={...meta,e